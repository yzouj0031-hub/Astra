# Astra · 给 Claude 的工作规则

这个文件是**每一轮都要照着执行的**，不是背景资料。

## 美术方向

正在从「低多边形方块原型」转向**有真实细节的风格**。参考图在 [`refs/`](./refs/)：

| 图 | 要从里面学什么 |
| --- | --- |
| `01-garden-diorama.jpg` | 红枫的低分叉与散开的冠、竹丛、细叶灌木、暖内光的夜景 |
| `02-misty-pine-ink.jpg` | 层叠松（迎客松）一盘一盘分层留空、蕾丝状的细碎叶、雾里的剪影与景深 |

参考图是**判断标准**，不是灵感来源。每轮结束要拿成品和它对着看。

## 硬性禁止

**不许用基础几何体拼树、人、车。** `BoxGeometry` / `SphereGeometry` / `CylinderGeometry` /
`ConeGeometry` 堆出来的树冠、人偶、车身，这条路线已经作废，不要再优化它，也不要"先用方块占位"。

正确的路子有两条，按情况选：

1. **加载现成的 CC0 glTF 模型**（Poly Pizza / Quaternius 等），重新打包控制体积。
2. **真枝干/骨架模型 + 按规则挂上去的 alpha 镂空卡片**，卡片图案用脚本生成。
   烟雨渡的柳、竹、层叠松走的就是这条（见 `journeys/watertown.js` 的 `plantUnderstory`）。

建筑、桥、船、家具这些**方盒子本来就合理的东西不在此列**，它们可以继续用几何体拼。

## 美术验收流程

改完任何看得见的东西，按顺序走完这五步，一步都不能省：

1. **截图**。用 `node tests/tree-shots.cjs <目录> [名字]` 或对应的截图脚本，
   拍的必须是真正跑起来的游戏，不是离线渲染的示意图。
2. **用 Read 工具把图看一遍**。不是"跑完截图脚本就算看过了" ——
   必须真的把 PNG 读进来用眼睛看。没看图就不算验收。
3. **对着 `refs/` 比**。同样用 Read 打开参考图，逐项对：叶子的粒度、树冠的分层、
   剪影、色调、密度。
4. **写差距清单**。把"参考图是什么样 / 现在是什么样"逐条列出来，交给用户。
   差距要写具体（"叶簇卡片，一棵只有几片" ），不许写"基本接近"这种话。
5. **有明显问题就不许宣布完成**。自己看着不对的地方，先修或先说，
   不能留给用户去发现。修不了的要明确说清为什么修不了、需要什么决定。

## 待办（按顺序，做完一条划掉一条）

这些是明确推迟的，不是忘了。**不要提前做**，也不要在别的轮次里顺手做掉。

- [ ] **星辉乐园的树要按距离降级。** 实测 169 棵树占 **99.2 万三角形**（每棵约 5900），
      是主世界最大的一块植被开销。手机端已经砍到四成，桌面端仍然偏重。
      安排：**人物那轮做完之后**回头补。做法待定，可选的有远处换低模、
      超过一定距离合并成 billboard、或按到相机的距离分批隐藏。
- [ ] **叶片的"蕾丝感"。** 现在的叶子仍是叶簇卡片，参考图 `refs/02-misty-pine-ink.jpg`
      那种能看出单叶和细枝的细度做不到。要补就得走"枝干 + 大量小叶片程序化撒布"，
      也就是柳树那套手法推广到所有树。安排：**人物、车船都做完之后**再回头。
- [ ] 细叶灌木仍是圆头的（`Bush`），比参考图粗。
- [ ] 层叠松的针盘密度够了但不够"实"，参考图里是成团的实体。
- [ ] 雾的浓度和层次离参考图那种水墨渐隐还差不少。
- [ ] **细化顺序（2026-09-14 用户定的）：① 剩下的船和电车 → ② 其他地区的路人 → ③ 建筑材质和细节。**
      每项做完给用户看游戏内改前改后实拍图。手机真机体验（帧率、发热、加载）这次没选，先不做。
- [ ] **还是方块的载具和人（2026-09-14 用户说「路人、车、飞机依旧很方块」）。** 已换：老爷车、水上飞机、
      维多利亚港路人与店员；① 船和电车（帆船、港区汽船与大眼鸡帆船、港区双层电车、雨港电车/河船/渡轮、
      烟雨渡乌篷船）也换完了。没换：乌篷船上的船夫、静屿码头边两条小划艇；
      静屿岛民、星辉乐园游客、雨港与烟雨渡路人 —— 这些是下一项 ②。港区轮船换之前先把它们排除出 instanceResortMeshes 的合批，
      否则藏不掉（路人就踩过这个坑，见 harborInstances.update 里的 crowdReplaced）。
- [ ] 路人女款长发（f_indigo_long）头顶露头皮，Quaternius 长发网格本身稀。
- [ ] **玩家角色换中式服装（C 方案，排在车船那轮之后 —— 2026-09-14 用户改了顺序，先做车、飞机、船）。** 现在接入的是 Quaternius 农夫装重染靛蓝，
      形制仍是欧式（肩带、带扣腰带、系带长靴）。要在 Blender 里把上衣改成长衫、去掉肩带、
      靴子换布鞋。**动手之前先出方案图给用户看**，定了再改。
- [ ] 玩家角色的脸偏西方（Quaternius 底模的五官）。不阻塞，记着。

## 别的

- 改动前先问清楚范围，做完如实报代价（体积、三角形数、帧率），数字要是实测的不是估的。
- 烟雨渡的镇子布局是固定种子的。动到随机序列会让宝塔、远山、菜畦、路人、船整片移位；
  新加的东西要么用独立随机流（`vrnd`），要么把种子按原步数推回去（`skipTo`）。
- 回退到旧的占位几何体**只允许发生在 `npm test` 的 Node 环境**（那里没有 XHR，加载不了 glTF）。
  浏览器里加载失败必须控制台报错 + 界面提示，不许静默显示旧资源。
